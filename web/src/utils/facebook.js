import * as API from './api';

/**
 * Send a request to the server to call the Facebook API to send a message to the user via the pages.
 * @param {*} message 
 * @param {*} audio 
 * @param {*} options 
 * @returns 
 */
export async function sendSampleReport(message, audio = false, options = {}) {
    // const res = await API.PostAPI('/api/facebook/reportsample', { message, audio, ...options });
    // return res;
}